<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <a class="navbar-brand" href="#">SymBNB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-chart-line menu-icons"></i>Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_ads_index') }}"><i class="fas fa-hotel menu-icons"></i>Annonces</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-book menu-icons"></i>Réservations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments_index') }}"><i class="fas fa-comments menu-icons"></i>Commentaires</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-users menu-icons"></i>Utilisateurs</a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="photo de profil de {{ app.user.fullName }}">
                        {{ app.user.fullName }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('homepage') }}" class="dropdown-item"><i class="fas fa-eye menu-icons"></i>Voir le site</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item"><i class="fas fa-sign-out-alt menu-icons"></i>Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>